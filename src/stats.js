//este archivo contiene la funcion para ver si los links estan rotos o no

//devuelve el largo del araray
const countLinks = (links) => {
  return links.length();
};

const countValidateLinks = (links) => {
    let countLinks
    for (const link of links) {
        if(link.status==='ok')
    }
};

module.exports = { countLinks };
